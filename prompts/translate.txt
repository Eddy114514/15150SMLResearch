Translate target.contract into Standard ML Boolean expressions that check an
input and its actual returned value. The target implementation is deliberately
not provided. Derive the checking algorithm from the stated property.

The course notation f(args) ==>* v means that the returned value is v. It
does not require calling f inside a predicate or proving runtime complexity.

Use target.input_pattern for input bindings. The variable result is the actual
return value, available only in ensures_expr. Bind helper variables and pure
recursive functions inside let expressions as needed. Use the documented
Basis operations and helper signatures. Do not call the target function or
load code, perform I/O, use mutable state, or catch exceptions to report success.

Preserve the contract exactly, including weak or false conditions. Do not add
requirements based on the function name. Do not repair the specification.
requires_expr checks only the precondition. ensures_expr checks every stated
postcondition assuming requires_expr is true.

Check every result branch. For SOME, verify the returned witness. For NONE,
verify the stated absence of a witness. Use exactly the domain in the contract;
do not invent a relationship between a returned list and an input list.
List equality preserves order and duplicates; multiset equality ignores order.
Quantified properties may use local finite search or justified mathematical
rewrites. Never replace a quantifier with an arbitrary sampled range.

Return one JSON object using the supplied schema:
- ok: requires_expr and ensures_expr are executable Boolean expressions;
  reason is empty.
- ambiguous: the contract has materially different possible meanings or an
  undefined term. Identify that wording and the unresolved meanings.
- unsupported: a stated requirement cannot be represented as an executable
  property, such as proving termination or asymptotic complexity. Identify it.

A missing target implementation, missing search algorithm, computational cost,
or absent convenience library function is not a reason to refuse translation.
Ordinary mathematical operations can be implemented using Basis combinators
or local pure functions. Compilation feedback concerns your generated code,
not the original specification.

For ambiguous or unsupported, both expressions are null. Return only JSON,
with no Markdown or explanation outside it.
