Repair only the syntax, typing, or unavailable-name errors in the generated
SML contract expressions, using the original contract as the authority.
Return the same JSON schema. Preserve all original conditions and do not
make the contract weaker or stronger to satisfy tests. You have no runtime
test results. Do not modify the wrapper, input types, allowed helpers, or
the implementation. If faithful repair is not possible within the supported
fragment, return ambiguous or unsupported as appropriate.

The target.contract in this request is the original specification. Your
previous assistant response is generated code, not part of that specification.
An unavailable function you introduced is a code-generation mistake: replace
it using the documented Basis operations or a local pure expression. It does
not make the original contract unsupported. A refusal must identify an obstacle
in target.contract itself, not an error in your previous response.

Read type errors using the supplied helper signatures. "Operator is not a
function" and "operator and operand don't agree" report incorrect application
or incompatible types, not an unavailable name. Check tuple versus curried
arguments and option versus contained value; repair the generated expression
while preserving its intended property. An unbound-name error is different.
